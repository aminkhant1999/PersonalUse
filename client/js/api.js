export async function api(path, options={}){const response=await fetch(`/api${path}`,{credentials:"same-origin",headers:{"Content-Type":"application/json",...(options.headers||{})},...options});let body;try{body=await response.json()}catch{throw new Error("The server returned an unreadable response.")}if(!response.ok)throw Object.assign(new Error(body.error?.message||"Request failed."),{fields:body.error?.fields,status:response.status});return body.data}
